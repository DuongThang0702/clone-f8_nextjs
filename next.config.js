/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "books.toscrape.com",
      "res.cloudinary.com",
      "files.fullstack.edu.vn",
    ],
  },
};

module.exports = nextConfig;
